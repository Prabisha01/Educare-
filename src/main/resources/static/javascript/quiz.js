const quizData=[
    {
        question: "Q1. What is correct syntax for main method of a java class?",
        a: "a. public static int main(String[] args)",
        b: "b. public int main(String[] args)",
        c: "c. public static void main(String[] args)",
        d: "d. None of the above.",
        correct: "c"
    },
    {
        question: "Q2. What is an Interface?",
        a: "a. An interface is a collection of abstract methods.",
        b: "b. Interface is an concrete class.",
        c: "c. Interface is an abstract class.",
        d: "d. None of the above.",
        correct: "b",
    },

    {
        question: "Q3. which operator is considered to be with highest precedence?",
        a: "a. () , []",
        b: "b.  =)",
        c: "c. ?",
        d: "d. %",
        correct: "a",
    },

    {
        question: "Q4. Which of the following are not Java keywords?",
        a: "a. Double",
        b: "b. switch",
        c: "c. then",
        d: "d. instanceof",
        correct: "c",
    },
    {
        question: "Q5. Which variables are created when an object is created with the use of the keyword 'new' and destroyed when the object is destroyed?",
        a: "a. Local variables",
        b: "b. Instance variables",
        c: "c. Class Variables",
        d: "d. Static variables",
        correct: "b",
    },
    {
        question: "Q6. What is byte code in Java?",
        a: "a. Code generated by a Java compiler.",
        b: "b. Code generated by a Java Virtual Machine",
        c: "c. Name of Java source code file",
        d: "d. Block of code written inside a class",
        correct: "a",
    },

];

const quiz = document.getElementById('quiz')
const answerEls = document.querySelectorAll('.answer')
const questionEl = document.getElementById('question')
const a_text = document.getElementById('a_text')
const b_text = document.getElementById('b_text')
const c_text = document.getElementById('c_text')
const d_text = document.getElementById('d_text')
const submitBtn = document.getElementById('submit')


let currentQuiz = 0
let score = 0

loadQuiz()


function loadQuiz(){

    deselectAnswers()

    const currentquizData = quizData[currentQuiz]

    questionEl.innerText = currentquizData.question
    a_text.innerText = currentquizData.a
    b_text.innerText = currentquizData.b
    c_text.innerText = currentquizData.c
    d_text.innerText = currentquizData.d
}

function deselectAnswers(){
    answerEls.forEach(answerEl => answerEl.checked = false)

}

function getSelected(){
    let answer
    answerEls.forEach(answerEl => {
        if(answerEl.checked){
            answer = answerEl.id
        }
    })
    return answer
}

submitBtn.addEventListener('click',()=>{
    const answer = getSelected()

    if(answer){
        if (answer == quizData[currentQuiz].correct){
            score++
        }
        currentQuiz++

        if(currentQuiz < quizData.length){
            loadQuiz()
        } else {
            quiz.innerHTML =`
            <h2>You answered ${score}/${quizData.length} question correctly</h2>
            `
            if (score<6){
                quiz.innerHTML+=`<button onclick="location.reload()">Reload</button>`
            } else{
                quiz.innerHTML+=`<button onclick="location.href= "certificate.html" >Generate certificate</button>`
            }
        }

    }
})

